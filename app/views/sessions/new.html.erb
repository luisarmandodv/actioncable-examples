<h1>Authenticate as one of the following users</h1>

<ul>
  <% @users.each do |user| %>
    <li> <%= user.id %> <%= link_to user.name, session_url(user_id: user.id), method: :post %></li>
  <% end %>
</ul>

